<div class="nav nav-admin">
  <%= t('.admin_panel') %>

  <span class="nav-admin user">
    <%= t('.welcome') %>,<b class="nav-admin user-email"><%= current_user.first_name %> <%= current_user.last_name %></b> Guru
    <%= button_to 'Logout', destroy_user_session_path, method: :delete %>
  </span>
</div>